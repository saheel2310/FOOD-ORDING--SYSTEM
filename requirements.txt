# Flask core framework - kept reasonably specific for stability
Flask==2.3.3

# Flask-SQLAlchemy - Allow newer patches/minor within 3.x series
Flask-SQLAlchemy>=3.1.1,<3.2

# Flask-Cors - Generally stable, kept specific for now
Flask-Cors==4.0.0

# python-dotenv - Stable, kept specific
python-dotenv==1.0.0

# SQLAlchemy - Allow newer patches/minor within 2.0.x series
SQLAlchemy>=2.0.20,<2.1